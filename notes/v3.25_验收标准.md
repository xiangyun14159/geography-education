# v3.25 验收标准

## AC-1: 使用 NASA 标准地图纹理

**修复方案**：
- 下载 Three.js 官方 Blue Marble 纹理（NASA 可见光地球卫星图），从 2048x1024 缩至 1024x512
- 保存为 JPEG（117 KB），确保不超过任何设备的 MAX_TEXTURE_SIZE
- `<img>` 标签加载，3 秒超时后回退到 procedural 纹理

**验收条件**：
- [x] 地球使用 `earth_tex.jpg`（NASA Blue Marble 标准配色：蓝海 + 绿棕大陆 + 白色云层/冰盖）
- [x] 纹理分辨率 1024x512（WebGL 安全尺寸，远低于所有设备的 MAX_TEXTURE_SIZE ≥ 2048）
- [x] `<img>` 加载失败时 3 秒内回退到 procedural 纹理（`setTimeout` + `fallbackTex`）
- [x] 无黑球/暗球（JPEG 正常加载 → CanvasTexture → WebGL）

## AC-2: 太阳视运动轨迹圈加大

**修复方案**：
- `updateSunPath()` 中 `dist` 从 0.18 增大到 0.27（`index.html:738`）
- `updateSunPathLabels()` 中 `dist` 从 0.24 增大到 0.36（`index.html:684`）

**验收条件**：
- [x] 太阳轨迹黄色圆环半径从 0.18 增大到 0.27（+50%）
- [x] 轨迹时间标签（0:00/6:00/12:00/18:00/24:00）距离从 0.24 增大到 0.36（+50%）
- [x] 轨迹和标签与球面无重叠（dist > _duckDist - 1.0 ≈ 0.027，充足余量）

---

## 审查结论

| 条目 | 状态 |
|------|------|
| AC-1 NASA Blue Marble 纹理 | ✅ 已修复 |
| AC-2 太阳轨迹圈加大 | ✅ 已修复 |

**修改文件**：`src/app/src/main/assets/index.html`（3 处编辑 + 新增 `earth_tex.jpg`）

**关键变更**：
- 新增 `assets/earth_tex.jpg`（1024x512, 117 KB, NASA Blue Marble via three.js）
- 纹理加载：恢复 `<img>` + IIFE + 超时回退模式，JPEG 体积小/尺寸安全
- `updateSunPath()` L738: `var dist=.27`（+50%, 原 .18）
- `updateSunPathLabels()` L684: `var dist=.36`（+50%, 原 .24）

**输出 APK**：`outputs/日地天文教学_v3.25_debug.apk`（358 KB，BUILD SUCCESSFUL）
